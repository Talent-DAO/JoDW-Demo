{
  "name": "@jodw/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service --access-token ACCESS_TOKEN --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ supriyaamisshra/lens-tdao-test-1",
    "create-local": "graph create --node http://localhost:8020/ talentdao/your-contract",
    "remove-local": "graph remove --node http://localhost:8020/ talentdao/your-contract",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 talentdao/your-contract"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.1",
    "@graphprotocol/graph-ts": "^0.22.1"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
