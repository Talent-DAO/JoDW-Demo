query EnabledModules @api(name: officiallens) {
  enabledModules {
    collectModules {
      moduleName
      contractAddress
    }
  }
  enabledModuleCurrencies {
    name
    symbol
    decimals
    address
  }
}